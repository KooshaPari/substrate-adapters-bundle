# Build outputs
/target/
**/target/
**/*.rs.bk

# Cargo lock files outside the workspace root (each shim is intentionally
# standalone-friendly and should not pollute the parent lock).
# We DO commit the root Cargo.lock.
/crates/*/Cargo.lock

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Tooling state
.pre-commit-cache/
.claude/